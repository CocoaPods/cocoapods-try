# Cocoapods::Try

CocoaPods plugin which allows to quickly try the demo project of a Pod.

## Installation

    $ git clone THIS_REPO_URL
    $ cd THIS_REPO
    $ rake install

## Usage

    $ pod try POD_NAME
